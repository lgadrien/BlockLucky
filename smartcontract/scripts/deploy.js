// Script de déploiement pour le contrat Lottery

async function main() {
  // Ton code de déploiement ici
}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
